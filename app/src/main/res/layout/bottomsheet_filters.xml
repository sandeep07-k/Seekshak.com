<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottomSheetRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:padding="16dp"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

        <!-- Header -->
        <TextView
            android:id="@+id/tvFilterTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Filter Tuition Posts"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Class Name -->
        <Spinner
            android:id="@+id/spinnerClass"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:entries="@array/class_list"
            app:layout_constraintTop_toBottomOf="@id/tvFilterTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Subject -->
        <Spinner
            android:id="@+id/spinnerSubject"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:entries="@array/subject_list"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/spinnerClass"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Mode -->
        <RadioGroup
            android:id="@+id/rgMode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/spinnerSubject"
            app:layout_constraintStart_toStartOf="parent">

            <RadioButton
                android:id="@+id/rbOnline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Online" />

            <RadioButton
                android:id="@+id/rbOffline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Offline" />
        </RadioGroup>

        <!-- Fee Range -->
        <TextView
            android:id="@+id/tvFeeRange"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Fee Range (₹)"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/rgMode"
            app:layout_constraintStart_toStartOf="parent"/>

        <SeekBar
            android:id="@+id/seekBarFee"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:max="5000"
            app:layout_constraintTop_toBottomOf="@id/tvFeeRange"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Gender -->
        <Spinner
            android:id="@+id/spinnerGender"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:entries="@array/gender_list"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/seekBarFee"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Posted Date -->
        <Spinner
            android:id="@+id/spinnerPostedDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:entries="@array/date_filter_list"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/spinnerGender"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Status -->
        <RadioGroup
            android:id="@+id/rgStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/spinnerPostedDate"
            app:layout_constraintStart_toStartOf="parent">

            <RadioButton
                android:id="@+id/rbActive"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Active" />

            <RadioButton
                android:id="@+id/rbInactive"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Inactive" />
        </RadioGroup>

        <!-- Buttons -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/rgStatus"
            app:layout_constraintEnd_toEndOf="parent">

            <Button
                android:id="@+id/btnReset"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Reset"/>

            <Button
                android:id="@+id/btnApply"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:text="Apply"/>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
